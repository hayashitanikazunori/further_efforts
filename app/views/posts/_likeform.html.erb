<p>いいね件数:<%= post.likes.count %></p>
<% if current_user.already_liked?(post) %>
  <%= button_to "いいね！を取り消す", post_like_path(post.id, current_user.id), method: :delete %>
<% else %>
  <%= button_to "いいね！", post_likes_path(post.id, current_user.id) %>
<% end %>